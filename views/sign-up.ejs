<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Sign Up</title>
  <link href="/output.css" rel="stylesheet">
</head>
<body class="min-h-screen bg-gray-900 text-white flex items-center justify-center p-6">
  <div class="w-full max-w-md bg-gray-800 p-6 rounded-2xl shadow">
    <h1 class="text-2xl font-bold mb-4">Create your account</h1>

    <% if (errors.length) { %>
      <ul class="mb-4 text-sm text-red-400 list-disc list-inside">
        <% errors.forEach(e => { %>
          <li><%= e.msg %></li>
        <% }) %>
      </ul>
    <% } %>

    <form action="/sign-up" method="POST" class="space-y-4">
      <div>
        <label class="block mb-1" for="first_name">First name</label>
        <input class="w-full rounded bg-gray-700 p-2"
               id="first_name" name="first_name" type="text"
               value="<%= old.first_name || '' %>">
      </div>

      <div>
        <label class="block mb-1" for="last_name">Last name</label>
        <input class="w-full rounded bg-gray-700 p-2"
               id="last_name" name="last_name" type="text"
               value="<%= old.last_name || '' %>">
      </div>

      <div>
        <label class="block mb-1" for="email">Email</label>
        <input class="w-full rounded bg-gray-700 p-2"
               id="email" name="email" type="email"
               value="<%= old.email || '' %>">
      </div>

      <div>
        <label class="block mb-1" for="password">Password</label>
        <input class="w-full rounded bg-gray-700 p-2"
               id="password" name="password" type="password">
      </div>

      <div>
        <label class="block mb-1" for="confirmPassword">Confirm password</label>
        <input class="w-full rounded bg-gray-700 p-2"
               id="confirmPassword" name="confirmPassword" type="password">
      </div>

      <button class="w-full bg-blue-600 hover:bg-blue-700 rounded p-2 font-semibold">
        Create account
      </button>
    </form>
  </div>
</body>
</html>